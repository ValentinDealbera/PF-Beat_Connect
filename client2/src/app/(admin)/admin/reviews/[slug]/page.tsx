import Content from "./content";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit Review | BeatConnect",
};

export default function Page() {
  return <Content />;
}
