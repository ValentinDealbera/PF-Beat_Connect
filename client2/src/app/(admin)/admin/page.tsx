import Content from "./content";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin | BeatConnect",
};

export default function SellerDashboardOverview() {
  return <Content />;
}
