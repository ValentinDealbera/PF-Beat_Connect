import Content from './_components/XContent'
import { type Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Logout | BeatConnect'
}

const Logout = () => <Content />

export default Logout
