import Content from './_components/Content'
import { type Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Recover | BeatConnect'
}

const Recover = () => <Content />

export default Recover
