import Content from "./content";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQs | BeatConnect",
};

export default function HelpOverview() {
  return <Content />;
}
